<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ow.tracer.mantuoluo.member.dsm.mapper.DetailsMapper">

	<!-- 字段与Model映射 -->


	<sql id="Base_Column_List">
		`nick_name`,
		`avatar_url`,
		`open_id`,
		`gender`,
		`user_type`
	'del_flag',
      'create_date',
      'create_by',
      'update_by',
      'update_date',
       'version_number',
       'enabled'
	</sql>

	<!--<select id="getByAll" resultType="details" parameterType="String">
		select d.id,member_id,d.tutor_id,d.payment,d.consultation_id,d.create_time
		from details as  d
		/*left join consultation as co on d.consultation_id=co.id
		left join tutor as t on d.tutor_id=t.id
		left join member_user as m on d.member_id=m.id*/
		where 1=1
		<if test="id != null">
			and d.id like CONCAT('%',#{id},'%')
		</if>
		<if test="memberId != null">
			and d.member_id like CONCAT('%',#{memberId},'%')
		</if>
		 <if test="tutorId != null">
             and d.tutor_id like CONCAT('%',#{tutorId},'%')
         </if>
		<if test="createTime!=null and createTime!=''">
			and DATE_FORMAT(d.create_time,'%Y/%m/%d')=DATE_FORMAT(#{createTime},'%Y/%m/%d')
		</if>

	</select>-->

</mapper>